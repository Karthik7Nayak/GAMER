import { BackgroundchangeDirective } from './backgroundchange.directive';

describe('BackgroundchangeDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundchangeDirective();
    expect(directive).toBeTruthy();
  });
});
